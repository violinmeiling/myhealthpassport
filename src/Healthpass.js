// src/Healthpass.js
import React from 'react';
import './Healthpass.css'; // Import the styles for the logo bar

const Healthpass = () => {
  return (
    <nav className="healthpass">
      <img src="/logo.png" alt="Logo" className="healthpass-logo" />
    </nav>
  );
};

export default Healthpass;
